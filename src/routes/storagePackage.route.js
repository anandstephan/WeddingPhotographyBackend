import express from 'express';
import { createStoragePackage } from '../controller/storagePackage.controller.js';

const storagePackageRouter = express.Router();


export default storagePackageRouter;